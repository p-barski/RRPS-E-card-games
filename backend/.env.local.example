# Copy to .env.local for local dev overrides (that file is gitignored).
# Every var here has a working default in settings.py already, except
# DATABASE_URL below — set explicitly so it's obvious how to swap it for
# Postgres in production

DATABASE_URL=sqlite:///db.sqlite3
